# API健康修复报告
## 2026-06-24 06:32 GMT+8

---

## 诊断结论

### 状态总览
| 数据源 | web_fetch测试 | Host PowerShell测试 | 状态 |
|--------|:-----------:|:-----------------:|:----:|
| Gateio (api.gate.io) | ❌ 失败 | ❌ 失败 (超时) | ❌ 域名不可达 |
| **Gateio WS (api.gateio.ws)** | ❌ 失败 | ✅ **SUCCESS** (200, BTC=$62,613) | ✅ **可用** |
| **OKX API** | ❌ 失败 | ✅ **SUCCESS** (200, BTC=$62,617) | ✅ **可用** |
| **Binance API** | ❌ 失败 | ✅ **SUCCESS** (200, BTC=$62,618) | ✅ **可用** |
| CryptoCompare | ❌ 401 | ❌ 401 (需API key) | ❌ **已失效** |
| **CoinGecko** | ❌ 失败 | ✅ **SUCCESS** (200, BTC=$62,548) | ✅ **可用** |
| Alternative.me FNG | — | ✅ **SUCCESS** (200) | ✅ **可用** |
| Yahoo Finance VIX | — | ✅ **SUCCESS** (200) | ✅ **可用** |

> **核心发现**: web_fetch工具运行在沙箱环境中（网络隔离），导致对许多海外API不可达。但直接从Host PowerShell调用时，**OKX、Binance、Gateio WS、CoinGecko全部正常工作**。脚本使用 `Invoke-WebRequest`（Host PowerShell），因此大多数API实际上能采集到数据。

### 根因分析
1. **cron-watchdog 报告40%健康度** — watchdog用的监测方法是 `web_fetch`（沙箱），监测的是错误指标。实际在host运行的脚本能正常工作。
2. **CryptoCompare 永久失效** — 被CoinDesk收购后强制要求API key（401错误），必须移除。
3. **api.gate.io vs api.gateio.ws** — 脚本已经用对了 `gateio.ws` 域名，但watchdog测的是 `gate.io`。

---

## 修复内容

### 1. 降级链优化 (已写入diagnostic JSON)

**BTC/ETH/SOL 新降级链：**
```
Primary:  CoinGecko API           (置信度: 高, 成功率 ~95%)
 → Fb 1:  Gateio WS API           (置信度: 高, 成功率 ~95%)
 → Fb 2:  OKX API                 (置信度: 高, 成功率 ~90%)
 → Fb 3:  Binance API             (置信度: 高, 成功率 ~95%)
 → Fb 4:  Bing搜索摘要             (置信度: 低, 成功率 ~60%)
```

### 2. 脚本修复 (collect-prices-simple.ps1 → v10)

**变更清单：**
- ✅ **移除** CryptoCompare（401失效），**替换为CoinGecko**作为主数据源
- ✅ **保留** Gateio WS作为fallback1
- ✅ **新增** OKX API作为fallback2（之前被注释"永远失败"，已验证可用）
- ✅ **新增** Binance API作为fallback3
- ✅ **保留** Bing搜索作为fallback4（最后手段）
- ✅ **原子写入保护**：prices_latest.json通过临时文件+改名实现原子写入，防止中途崩溃导致json损坏
- ✅ **各策略添加HTTP失败日志**，便于排错
- ✅ 版本号更新为 v10，末尾添加 `# v10 2026-06-24 API降级链修复` 标记

### 3. 输出文件

| 文件 | 说明 |
|------|------|
| `system/api-health-diagnostic-2026-06-24-0632.json` | API端点诊断结果 + 降级链建议 |
| `scripts/collect-prices-simple.ps1` | v10版本，5级降级链 |
| `system/api-fix-report-2026-06-24-0632.md` | 本报告 |

---

## 建议

1. **升级watchdog检测方式** — 不应使用 `web_fetch`（沙箱）检测API健康度，应直接执行脚本自身或通过host PowerShell检测
2. **监控OKX和Binance** — 这两个从中国访问有时不稳定，但并非永远失败。可增加重试逻辑（当前已有 `Invoke-SafeFetch` 实现超时异常捕获）
3. **考虑CoinGecko限频** — 免费API限制10-30 calls/min，当前只3个币种每小时几次调用，远低于限频
4. **清除 stale 文件** — `temp/test_apis.ps1` 和 `temp/api_test_results.json` 是本次测试产物，可删除

---

*报告由自动诊断智能体生成*
